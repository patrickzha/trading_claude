# 工作流规则（自动加载，每次都要遵守，不是可选参考）

这个文件是为了修复一个反复出现的真实漏洞而写的：之前多次在写完一段总结/汇报之后，
没有真的启动下一步任务，只是用文字描述"接下来要做什么"，导致整个流程停在那里
干等，用户不得不主动来问"你在干嘛"才能把流程叫醒。写"我会检查""我保证"这类
承诺本身没有约束力，因为那只是又一段生成的文字，不会真正拦住下一次输出。

## 核心工作流程（用户明确定义，必须严格按这个格式，不是自由发挥）

1. 每一轮开始，提出**6条建议**：**2条大修大补**（真正需要大量新代码/新数据/
   新框架的改动，不是加个参数或者改个阈值——量级要对得上"大修大补"这四个字）
   + **4条小修小补**（真正可能提升项目的具体检验/修复，不是凑数、不是纯文档
   整理、不是"检查交叉引用"这类不影响项目本身能力的行政性任务）。
2. 自己给这6条排优先级。
3. 依次实现每一条，并且真的跑起来验证结果（不是纸上谈兵），如实记录结论
   （包括测出来是负面结果、不采纳的情况——负面结果一样要写清楚，不能因为
   没达到预期就淡化处理）。
4. 6条全部做完、验证完，立刻提出下一轮新的6条（2+4），不需要用户批准，
   不需要停下来问，除非用户明确说"停"。
5. 循环，没有上限，一直做到用户叫停为止。

**什么不算"小修小补"，不能塞进这4条名额里**：更新README的"已知限制"章节、
检查文档交叉引用、整理文件清单、格式化代码——这些是维护性工作，做的时候
顺手做掉就行，不需要占用6条建议的名额、也不需要单独立项。6条名额只能是
"跑了之后会得到一个新的、关于项目本身是否更好用的结论"这种实质性工作。

**大修大补的框架要求（用户2026-08明确要求）**：每一轮的2条大修大补里，
至少要有1条是真正的框架改动——引入一个跟现有技术特征框架(21维技术指标+
XGBoost回归)不同的新范式(新的信号来源、新的资产类别、新的模型架构、新的
数据维度)，不能是在同一套框架里反复调参数/换组合方式。这次会话已经反复
验证过21维技术特征在这个问题上信息量趋近于零(pred_ret跟net_ret相关系数
接近0、按预测值分档不单调、多种训练目标都不通)，继续在这个框架内部找
新组合大概率是浪费时间；只有跳出框架本身（比如质量因子、期权隐含信息、
完全不同的资产类别）才可能有真正的新发现。这条约束从提出时的这一轮开始
生效，之前几轮的大修大补不需要回溯改动。

## 硬性规则

1. **先启动动作，再写总结。** 顺序不能反过来。要汇报进展的时候，先用工具调用
   启动下一步要做的事(后台任务+挂watcher)，确认它真的在跑（用`ps aux`之类的
   命令核实，不是自己说了算），然后再写总结文字描述刚完成的工作。总结这时候
   只是在描述一个已经在跑的东西，不是在承诺一件还没发生的事。

2. **禁止"纯文字总结"的回复。** 只要写总结，这条回复里必须同时包含至少一个
   工具调用（新任务的启动，或者已有任务的核实）。不允许出现"这一条回复只有
   文字、没有任何工具调用"这种形态。

3. **每一轮收尾前的检查**：这一轮结尾是不是满足"有真实在跑的后台任务+已经
   挂好的watcher"，或者"这次要求的事真的全部做完"这两种情况之一？两者都不
   满足，就是又要犯错，必须先补一个工具调用，不能用文字收尾。

4. **每小时的独立兜底（用户明确要求）**：用ScheduleWakeup机制设置1小时的
   安全网检查——不管前面的流程是否正常，1小时后自动唤醒一次，用`ps aux`确认
   有没有相关的后台任务在跑，没有就立刻启动下一项有价值的工作。这是独立于
   Bash watcher之外的第二层保险，每次流程重新开始或者长时间运行时都要重新
   挂一个（因为ScheduleWakeup只触发一次，触发后如果还要继续兜底，需要在
   那次唤醒里重新挂下一个）。

5. **总结要短。** 长总结本身会积累"写完了、该收尾了"的叙事惯性，越长这个
   拉力越强。汇报进展控制在几句话，不追求面面俱到。

## 项目特有的安全规则（沿用之前会话建立的）

- 绝对不要在`us_stock_screener.py`/`backtest.py`有ProcessPoolExecutor任务
  正在运行时修改这两个文件——spawn方式的worker会用新代码+主进程传来的旧
  config dict，字段对不上会静默产生KeyError被吞掉，选股结果会悄悄退化成
  空仓，非常难发现。
- 监控后台任务不能只看进程是否存活，要看日志内容是否真的在增长/推进。
- 全程保持`caffeinate -i -s`不间断，定期用`ps -p <caffeinate_pid>`核实。
